name: my_project_baws
description: "A new Flutter project."

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1






environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
 


  # Add any additional dependencies your app requires
  cupertino_icons: ^1.0.2
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  firebase_storage: ^12.1.0
  icons_plus: ^5.0.0
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.0.17
  path_provider: ^2.0.12
  image_picker: ^1.0.7
  widget_zoom: ^0.0.4
  font_awesome_flutter: 
  
  
  google_fonts: ^6.2.1

  
  flex_color_scheme: ^7.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Add any additional dev dependencies your app requires
  flutter_lints: ^4.0.0
  flutter_launcher_icons: "^0.13.1"
   
flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/icon/icon.png"

flutter_native_splash: 
 color: "#42a5f5"  
  






flutter:
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS-style icons.
  uses-material-design: true

  # Add any additional assets your app requires assets:
   
  assets: 
    - assets/images/
  
  

  # Add any custom fonts your app requires
 